; PlatformIO project for the ESP32-CAM vision co-processor (Phase 1B).
; Sends compact face/emotion results to the main board over UART.

[env:cam-coproc]
platform = espressif32
framework = arduino
board = esp32cam
monitor_speed = 115200
build_flags =
    -D CORE_DEBUG_LEVEL=3
lib_deps =
